<Application x:Class="Clock_WPF.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:Clock_WPF"
             StartupUri="MainWindow.xaml">
    <Application.Resources>
        <Storyboard x:Key="ButtonEnter">
            <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                                 From="0.7" To="1" Duration="0:0:0.2" />
            <DoubleAnimation Storyboard.TargetProperty="Width"
                                                 From="30" To="32.5" Duration="0:0:0.2" />
            <DoubleAnimation Storyboard.TargetProperty="Height"
                                                 From="30" To="32.5" Duration="0:0:0.2" />
        </Storyboard>
        <Storyboard x:Key="ButtonLeave">
            <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                                 From="1" To="0.7" Duration="0:0:0.2" />
            <DoubleAnimation Storyboard.TargetProperty="Width"
                                                 From="32.5" To="30" Duration="0:0:0.2" />
            <DoubleAnimation Storyboard.TargetProperty="Height"
                                                 From="32.5" To="30" Duration="0:0:0.2" />
        </Storyboard>

        <!-- Button Style -->
        <Style TargetType="{x:Type Button}">
            <Setter Property="Background" Value="White"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Border CornerRadius="25" Background="{TemplateBinding Background}" BorderBrush="Transparent" BorderThickness="0">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#00000000"/>
                </Trigger>
            </Style.Triggers>
        </Style>

    </Application.Resources>
</Application>
